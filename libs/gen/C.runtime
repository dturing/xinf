/* this file is generated with nekobind. do not modify it direcly. */
/*  (unless you're looking at 'C.runtime') */

#define HEADER_IMPORTS
#include <neko.h>
#include "cptr.h"

vkind k_cptr;

CPTR_LOCAL_HELPERS

DEFINE_ENTRY_POINT(my_main);
void my_main() {
	k_cptr = kind_import("cptr");
	if( !k_cptr ) {
		DEFINE_KIND(_k_cptr);
		kind_export(_k_cptr,"cptr");
		k_cptr = kind_import("cptr");
	}
}
